<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frybits.harmonyprefs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".test.singleentry.commit.HarmonyPrefsCommitActivity" />

        <service
            android:name=".test.singleentry.commit.HarmonyPrefsCommitFooService"
            android:process=":foo" />

        <activity android:name=".test.singleentry.apply.HarmonyPrefsApplyActivity" />

        <service
            android:name=".test.singleentry.apply.HarmonyPrefsApplyFooService"
            android:process=":foo" />
    </application>
</manifest>
